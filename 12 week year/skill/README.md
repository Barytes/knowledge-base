# 本项目的 Skills

本目录只存放服务于本 12 Week Year 项目的 skills。每个 skill 使用一个独立子目录，以 `SKILL.md` 为入口；脚本、参考资料和测试均归该 skill 自己的子目录。

## 已有 Skill

| Skill | 用途 | 入口 |
| --- | --- | --- |
| freeform-12wy-sync | 从无边记单向同步 12 Week Year 内容，保留原文并检查冲突 | [SKILL.md](freeform-12wy-sync/SKILL.md) |

这里是用户指定的项目存放位置，不是全局安装目录。项目 `AGENTS.md` 会把匹配的任务路由到相应入口；也可以在请求中直接指定该 `SKILL.md`。未创建全局安装或 `.agents/skills` 链接，不据此声称已出现在应用的 skill 选择器中。
