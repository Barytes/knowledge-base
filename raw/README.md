# Raw Sources

`raw/` is the source-of-truth layer.

- `external/` is for articles, papers, reports, clips, and other outside materials.
- `personal/` is for journals, conversations, and your own writing.

Files in `raw/` should be treated as immutable evidence. The agent may read them and add links or metadata only when explicitly allowed.
