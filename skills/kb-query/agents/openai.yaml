interface:
  display_name: "KB Query"
  short_description: "Answer from the local knowledge base."
  default_prompt: "Use $kb-query to answer this question from the local knowledge base only, without automatic web search."
